<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"    android:layoutDirection="ltr">


    <Button
        android:tint="#ff5e5a"
        android:id="@+id/rezerve"
        android:layout_width="85dp"
        android:layout_height="85dp"
        android:background="@android:drawable/ic_menu_directions"
        android:layout_alignParentRight="true"
        android:layout_margin="30dp"
        />
    <TextView
        android:id="@+id/meetplacename"
        android:textSize="22dp"
        android:textColor="#ff5e5a"
        android:text="placeto meet"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="28dp"
        android:layout_alignBottom="@+id/rezerve"
        android:layout_toStartOf="@+id/rezerve"></TextView>
</RelativeLayout>